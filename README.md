# podman-pihole
A docker-compose file that composes up pihole in a podman container. 

# Requirements 
This requires systemd-networkd managed interfaces on the host, first set up br0 bridge to 
your physical interface.
